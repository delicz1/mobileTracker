@model IEnumerable<MobileTracker.Models.EventType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Icon)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Icon)
        </td>
        <td>
            @Html.ActionLink("Editovat", "Edit", new { id=item.EventTypeId }) |
            @Html.ActionLink("Detail", "Details", new { id=item.EventTypeId }) |
            @Html.ActionLink("Smazat", "Delete", new { id=item.EventTypeId })
        </td>
    </tr>
}

</table>
